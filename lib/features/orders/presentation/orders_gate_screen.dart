import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:lucide_icons/lucide_icons.dart';
import '../../../app/providers.dart';
import '../../../core/theme.dart';
import '../../../core/widgets/async_state_widgets.dart';
import '../../checkout/data/commerce_repository.dart';
import 'orders_screen.dart';
class OrdersGateScreen extends ConsumerStatefulWidget{const OrdersGateScreen({super.key});@override ConsumerState<OrdersGateScreen> createState()=>_OrdersGateScreenState();}
class _OrdersGateScreenState extends ConsumerState<OrdersGateScreen>{bool previous=false;bool _done(String status)=>const {'delivered','returned','rejected'}.contains(status);bool _needsReceipt(OrderModel o){final m=o.paymentMethod.toLowerCase(),s=o.paymentStatus.toLowerCase();return (m=='transfer'||m=='wallet')&&const {'awaiting_receipt','receipt_rejected'}.contains(s)&&!_done(o.status);}bool _receiptPending(OrderModel o){final m=o.paymentMethod.toLowerCase(),s=o.paymentStatus.toLowerCase();return (m=='transfer'||m=='wallet')&&s=='pending_review';}
 @override Widget build(BuildContext context){final session=ref.watch(hasSessionProvider);return Scaffold(body:SafeArea(child:session.when(loading:()=>const SpikeLoading(),error:(_,__)=>_LoginRequired(onLogin:()=>context.push('/login?next=%2Forders')),data:(loggedIn)=>loggedIn?_orders(context):_LoginRequired(onLogin:()=>context.push('/login?next=%2Forders')))));}
 Widget _head(BuildContext context,String title)=>Padding(padding:const EdgeInsets.fromLTRB(SpikeSpacing.page,SpikeSpacing.sm,SpikeSpacing.page,0),child:SizedBox(height:60,child:Stack(alignment:Alignment.center,children:[Text(title,style:const TextStyle(fontSize:21,fontWeight:FontWeight.w700)),Align(alignment:Alignment.centerRight,child:SizedBox(width:50,height:40,child:IconButton(onPressed:()=>context.canPop()?context.pop():context.go('/profile'),icon:const Icon(LucideIcons.arrowRight,size:22))))])));
 Widget _orders(BuildContext context){final state=ref.watch(ordersProvider),dark=Theme.of(context).brightness==Brightness.dark;return Column(children:[_head(context,'طلباتي'),Padding(padding:const EdgeInsets.fromLTRB(SpikeSpacing.page,0,SpikeSpacing.page,18),child:Row(children:[_tab('الحالية',!previous,()=>setState(()=>previous=false),dark),const SizedBox(width:9),_tab('السابقة',previous,()=>setState(()=>previous=true),dark)])),Expanded(child:state.when(loading:()=>const SpikeLoading(),error:(error,_)=>SpikeErrorState(message:_friendlyError(error),onRetry:()=>ref.invalidate(ordersProvider)),data:(orders){final list=orders.where((o)=>previous?_done(o.status):!_done(o.status)).toList();if(list.isEmpty)return SpikeEmptyState(message:previous?'لا توجد طلبات سابقة حتى الآن':'طلباتك الجديدة ستظهر هنا');return RefreshIndicator(onRefresh:()async{ref.invalidate(ordersProvider);await ref.read(ordersProvider.future);},child:ListView.separated(padding:const EdgeInsets.fromLTRB(SpikeSpacing.page,0,SpikeSpacing.page,SpikeSpacing.xl),itemCount:list.length,separatorBuilder:(_,__)=>const SizedBox(height:13),itemBuilder:(context,index)=>_OrderCard(order:list[index],dark:dark,needsReceipt:_needsReceipt(list[index]),receiptPending:_receiptPending(list[index]))));}))]);}
 String _friendlyError(Object e){final raw=e.toString().toLowerCase();return raw.contains('unauthorized')||raw.contains('401')?'انتهت جلسة تسجيل الدخول. سجّل الدخول مرة أخرى.':e.toString();}
 Widget _tab(String label,bool active,VoidCallback onTap,bool dark)=>SizedBox(width:88,height:39,child:FilledButton(style:FilledButton.styleFrom(elevation:0,padding:EdgeInsets.zero,backgroundColor:active?(dark?Colors.white:Colors.black):(dark?spikeDarkPanel:const Color(0xFFE7E7E7)),foregroundColor:active?(dark?Colors.black:Colors.white):Theme.of(context).colorScheme.onSurface,shape:RoundedRectangleBorder(borderRadius:BorderRadius.circular(22))),onPressed:onTap,child:Text(label,style:const TextStyle(fontSize:11,fontWeight:FontWeight.w700))));}
class OrderDetailsGateScreen extends ConsumerWidget{const OrderDetailsGateScreen({super.key,required this.id});final String id;@override Widget build(BuildContext context,WidgetRef ref){final session=ref.watch(hasSessionProvider);return session.when(loading:()=>const Scaffold(body:SafeArea(child:SpikeLoading())),error:(_,__)=>Scaffold(body:SafeArea(child:_LoginRequired(onLogin:()=>context.push('/login?next=${Uri.encodeComponent('/order/$id')}')))),data:(loggedIn)=>loggedIn?OrderDetailsScreen(id:id):Scaffold(body:SafeArea(child:_LoginRequired(onLogin:()=>context.push('/login?next=${Uri.encodeComponent('/order/$id')}')))));}}
class _OrderCard extends StatelessWidget{const _OrderCard({required this.order,required this.dark,required this.needsReceipt,required this.receiptPending});final OrderModel order;final bool dark,needsReceipt,receiptPending;@override Widget build(BuildContext context){final split=order.spikeWalletAmount>0&&order.remainingAmount>0;return InkWell(onTap:()=>context.push('/order/${order.id}'),borderRadius:BorderRadius.circular(24),child:Container(width:double.infinity,padding:const EdgeInsets.all(14),decoration:BoxDecoration(color:dark?spikeDarkPanel:const Color(0xFFE9E9E9),borderRadius:BorderRadius.circular(24)),child:Column(crossAxisAlignment:CrossAxisAlignment.stretch,children:[Row(crossAxisAlignment:CrossAxisAlignment.start,children:[Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(order.stores.length>1?'${order.stores.length} متاجر':_statusLabel(order.status),style:const TextStyle(fontSize:13,fontWeight:FontWeight.w700)),if(order.displayDate.isNotEmpty)Padding(padding:const EdgeInsets.only(top:2),child:Text(order.displayDate,style:const TextStyle(fontSize:9,color:spikeMuted)))])),Directionality(textDirection:TextDirection.ltr,child:Text('#${_short(order.id)}',style:const TextStyle(fontSize:12,fontWeight:FontWeight.w700)))]),if(order.stores.isNotEmpty)...[const SizedBox(height:10),Wrap(spacing:6,runSpacing:6,children:order.stores.map((s)=>Container(padding:const EdgeInsets.symmetric(horizontal:8,vertical:5),decoration:BoxDecoration(color:Theme.of(context).colorScheme.surface,borderRadius:BorderRadius.circular(12)),child:Row(mainAxisSize:MainAxisSize.min,children:[const Icon(LucideIcons.store,size:12),const SizedBox(width:4),ConstrainedBox(constraints:const BoxConstraints(maxWidth:120),child:Text(s.storeName,maxLines:1,overflow:TextOverflow.ellipsis,style:const TextStyle(fontSize:9,fontWeight:FontWeight.w800))),const SizedBox(width:5),Text(_statusLabel(s.status),style:const TextStyle(fontSize:8,color:spikeMuted,fontWeight:FontWeight.w700))])).toList())],if(needsReceipt)...[const SizedBox(height:10),_ReceiptBadge(color:spikeRed,icon:LucideIcons.receipt,text:order.paymentMethod=='wallet'?'سند المحفظة مطلوب':'سند الحوالة مطلوب'),const SizedBox(height:6),Text(order.paymentMethod=='wallet'?'ارفع سند دفع المحفظة لإكمال المراجعة.':'ارفع سند الحوالة لإكمال مراجعة الدفع.',style:const TextStyle(fontSize:10,color:spikeRed))]else if(receiptPending)...[const SizedBox(height:10),const _ReceiptBadge(color:Color(0xFFC47A00),icon:LucideIcons.clock3,text:'السند قيد المراجعة')],const Padding(padding:EdgeInsets.symmetric(vertical:11),child:Divider(height:1)),Row(children:[Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(split?'محفظة سبايك + ${_paymentMethodLabel(order.paymentMethod)}':_paymentMethodLabel(order.paymentMethod),style:const TextStyle(fontSize:10,fontWeight:FontWeight.w700)),Text(_paymentStatusLabel(order.paymentStatus),style:const TextStyle(fontSize:9,color:spikeMuted))])),Text('${order.total.toStringAsFixed(2)} ${order.currencyCode}',style:const TextStyle(fontSize:13,fontWeight:FontWeight.w800)),const SizedBox(width:8),const Icon(LucideIcons.chevronLeft,size:18,color:spikeMuted)])])));}
 static String _short(String id)=>id.length<=8?id:id.substring(0,8);static String _statusLabel(String s)=>const {'pending_admin_review':'قيد مراجعة الإدارة','approved':'تم اعتماد الطلب','accepted':'تم التأكيد','processing':'قيد التجهيز','ready_for_delivery':'جاهز للتوصيل','assigned':'تم تعيين مكتب التوصيل','picked_up':'تم الاستلام من المتجر','with_courier':'مع المندوب','delivered':'تم التوصيل','returned':'مرتجع','rejected':'مرفوض'}[s]??s;static String _paymentMethodLabel(String m)=>const {'transfer':'حوالة مالية','cod':'الدفع عند الاستلام','wallet':'محفظة إلكترونية','spike_wallet':'محفظة سبايك'}[m]??m;static String _paymentStatusLabel(String s)=>const {'awaiting_receipt':'بانتظار رفع السند','pending_review':'السند قيد المراجعة','receipt_rejected':'السند يحتاج إعادة رفع','approved':'تم اعتماد الدفع','paid':'تم دفع المبلغ','pending_collection':'الدفع عند الاستلام','cod_confirmed':'تم تأكيد الطلب'}[s]??s;}
class _ReceiptBadge extends StatelessWidget{const _ReceiptBadge({required this.color,required this.icon,required this.text});final Color color;final IconData icon;final String text;@override Widget build(BuildContext context)=>Align(alignment:Alignment.centerRight,child:Container(padding:const EdgeInsets.symmetric(horizontal:10,vertical:6),decoration:BoxDecoration(color:color.withValues(alpha:.10),borderRadius:BorderRadius.circular(16),border:Border.all(color:color.withValues(alpha:.32))),child:Row(mainAxisSize:MainAxisSize.min,children:[Icon(icon,size:14,color:color),const SizedBox(width:5),Text(text,style:TextStyle(fontSize:10,color:color,fontWeight:FontWeight.w800))])));}
class _LoginRequired extends StatelessWidget{const _LoginRequired({required this.onLogin});final VoidCallback onLogin;@override Widget build(BuildContext context)=>Center(child:Padding(padding:const EdgeInsets.symmetric(horizontal:34),child:Column(mainAxisSize:MainAxisSize.min,children:[const Icon(LucideIcons.packageSearch,size:44),const SizedBox(height:14),const Text('سجّل الدخول لعرض طلباتك',textAlign:TextAlign.center,style:TextStyle(fontSize:18,fontWeight:FontWeight.w800)),const SizedBox(height:7),const Text('تابع طلباتك الحالية والسابقة من حسابك.',textAlign:TextAlign.center,style:TextStyle(fontSize:11,color:spikeMuted,height:1.5)),const SizedBox(height:18),SizedBox(width:double.infinity,height:43,child:FilledButton(style:FilledButton.styleFrom(backgroundColor:spikeRed),onPressed:onLogin,child:const Text('تسجيل الدخول',style:TextStyle(fontWeight:FontWeight.w700))))])));}
